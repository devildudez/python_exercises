# Обзор алгоритмов сортировки

> **Урок 3** | 22.12.25 | 14:10 МСК (18:10 THA)

---

## Что такое сортировка?

**Сортировка** — упорядочивание элементов по определённому критерию (числа по возрастанию, строки по алфавиту и т.д.).

Большинство классических алгоритмов — **сравнивающие сортировки**: они сравнивают элементы между собой. Теоретический
предел для таких алгоритмов — **O(n·log n)** в среднем/худшем случае.

---

## Группы алгоритмов сортировки

### 1️⃣ Простые «квадратичные» алгоритмы — O(n²)

| Алгоритм           | Описание                                                             |
|--------------------|----------------------------------------------------------------------|
| **Bubble sort**    | Сравнивает соседние элементы и меняет местами, пока не отсортировано |
| **Selection sort** | Ищет минимум в неотсортированной части и ставит на место             |
| **Insertion sort** | Вставляет элемент в нужную позицию отсортированной части             |

**Когда использовать:** обучение, очень маленькие массивы (<20 элементов).

---

### 2️⃣ Быстрые алгоритмы — O(n·log n)

| Алгоритм       | Описание                                               |
|----------------|--------------------------------------------------------|
| **Merge sort** | Разделяй и властвуй: делит пополам, сортирует, сливает |
| **Quick sort** | Выбирает опорный элемент, разделяет массив на части    |
| **Heap sort**  | Строит кучу (heap), извлекает максимум                 |
| **Shell sort** | Улучшенный Insertion sort с переменным шагом           |
| **Timsort**    | Гибрид Merge + Insertion (стандарт Python)             |

**Когда использовать:** большие массивы, продакшн-код.

---

### 3️⃣ Специализированные алгоритмы — O(n) или около

| Алгоритм          | Описание                                              |
|-------------------|-------------------------------------------------------|
| **Counting sort** | Считает количество каждого значения (для целых чисел) |
| **Radix sort**    | Сортирует по разрядам (цифрам)                        |
| **Bucket sort**   | Распределяет по "корзинам", сортирует каждую          |
| **Tree sort**     | Строит BST дерево, обходит in-order                   |

**Когда использовать:** ограниченный диапазон значений, специфичные данные.

---

## Сравнительная таблица (полная)

| Алгоритм      | Лучшее     | Среднее            | Худшее         | Память   | Стабильный | В Python        |
|---------------|------------|--------------------|----------------|----------|------------|-----------------|
| Bubble        | O(n)       | O(n²)              | O(n²)          | O(1)     | ✅ Да       | ❌ Нет           |
| Selection     | O(n²)      | O(n²)              | O(n²)          | O(1)     | ❌ Нет      | ❌ Нет           |
| Insertion     | O(n)       | O(n²)              | O(n²)          | O(1)     | ✅ Да       | ⚡ Часть Timsort |
| Merge sort    | O(n·log n) | O(n·log n)         | O(n·log n)     | O(n)     | ✅ Да       | ⚡ Часть Timsort |
| Quick sort    | O(n·log n) | O(n·log n)         | O(n²)          | O(log n) | ❌ Нет      | ❌ Нет           |
| Heap sort     | O(n·log n) | O(n·log n)         | O(n·log n)     | O(1)     | ❌ Нет      | `heapq` модуль  |
| Shell sort    | O(n·log n) | O(n^1.25)–O(n^1.5) | O(n²)          | O(1)     | ❌ Нет      | ❌ Нет           |
| Counting sort | O(n+k)     | O(n+k)             | O(n+k)         | O(k)     | ✅ Да       | ❌ Нет           |
| Radix sort    | O(n·k)     | O(n·k)             | O(n·k)         | O(n+k)   | ✅ Да       | ❌ Нет           |
| Bucket sort   | O(n+k)     | O(n+k)             | O(n²)          | O(n+k)   | ✅/❌        | ❌ Нет           |
| Tree sort     | O(n·log n) | O(n·log n)         | O(n²)          | O(n)     | ✅ Да       | ❌ Нет           |
| **Timsort**   | **O(n)**   | **O(n·log n)**     | **O(n·log n)** | **O(n)** | **✅ Да**   | **✅ Стандарт**  |

### Расшифровка колонок:

- **Лучшее/Среднее/Худшее** — временная сложность
- **Память** — дополнительная память (auxiliary space)
- **Стабильный** — сохраняет порядок равных элементов

---

## Python: встроенная сортировка

### `sorted()` и `list.sort()`

Python использует **Timsort** — гибридный алгоритм, созданный Тимом Петерсом в 2002 году.

```python
# Создаёт новый список
sorted_list = sorted([3, 1, 2])  # [1, 2, 3]

# Сортирует на месте (in-place)
my_list = [3, 1, 2]
my_list.sort()  # my_list теперь [1, 2, 3]
```

### Почему Timsort так хорош?

| Свойство       | Значение                                    |
|----------------|---------------------------------------------|
| Лучший случай  | O(n) — почти отсортированные данные         |
| Средний/худший | O(n·log n)                                  |
| Стабильность   | ✅ Да                                        |
| Адаптивность   | Использует уже упорядоченные участки (runs) |

### Параметры сортировки

```python
# По ключу
sorted(["banana", "apple", "cherry"], key=len)  # ['apple', 'banana', 'cherry']

# В обратном порядке
sorted([1, 3, 2], reverse=True)  # [3, 2, 1]

# Комбинация
students = [("Alice", 85), ("Bob", 90), ("Charlie", 85)]
sorted(students, key=lambda x: x[1], reverse=True)
# [('Bob', 90), ('Alice', 85), ('Charlie', 85)]
```

---

## Библиотеки Python для сортировки

### 1. `heapq` — Heap sort операции

```python
import heapq

nums = [3, 1, 4, 1, 5]
heapq.heapify(nums)  # Превращает в кучу O(n)
heapq.heappop(nums)  # Извлекает минимум O(log n)
heapq.nsmallest(3, nums)  # 3 наименьших элемента
```

### 2. `bisect` — Бинарный поиск для отсортированных списков

```python
import bisect

sorted_list = [1, 3, 5, 7]
bisect.insort(sorted_list, 4)  # [1, 3, 4, 5, 7]
```

### 3. `numpy.sort()` — Для массивов NumPy

```python
import numpy as np

arr = np.array([3, 1, 2])
np.sort(arr)  # Использует Quicksort/Mergesort/Heapsort
```

---

## Когда что использовать?

| Ситуация                       | Рекомендация                             |
|--------------------------------|------------------------------------------|
| Обычная задача                 | `sorted()` / `.sort()` — всегда          |
| Нужен только min/max или top-K | `heapq.nsmallest()` / `heapq.nlargest()` |
| NumPy массивы                  | `np.sort()`                              |
| Учебные цели                   | Реализовать вручную Selection/Insertion  |
| Целые числа в узком диапазоне  | Counting sort (вручную)                  |

---

## Итог

> **В 99% случаев используй встроенный `sorted()` или `.sort()`** — Timsort оптимизирован для реальных данных и
> покрывает большинство сценариев.

Ручная реализация нужна для:

- Понимания алгоритмов (обучение)
- Специфичных задач с ограничениями
- Собеседований 😄
