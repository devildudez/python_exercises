# Простые «квадратичные» сортировки — O(n²)

Это базовые алгоритмы сортировки, которые легко понять и реализовать. Их временная сложность O(n²), поэтому они подходят только для небольших массивов.

---

## Обзор трёх алгоритмов

| Алгоритм | Метафора | Сложность | Стабильный |
|----------|----------|-----------|------------|
| **Bubble sort** | Пузыри всплывают наверх | O(n²) | ✅ Да |
| **Selection sort** | Выбираем минимум и ставим на место | O(n²) | ❌ Нет |
| **Insertion sort** | Вставляем карту в отсортированную руку | O(n²) | ✅ Да |

---

## 1. Bubble Sort (Пузырьковая сортировка)

**Идея:** Сравниваем соседние элементы и меняем местами, если они в неправильном порядке. Повторяем, пока массив не отсортирован.

**Метафора:** Как пузырьки воздуха — большие элементы "всплывают" в конец массива.

> ⚠️ *Пример кода не приводится — разбирали на прошлом уроке*

---

## 2. Selection Sort (Сортировка выбором)

**Идея:** Делим массив на две части — отсортированную (слева) и неотсортированную (справа). На каждом шаге находим **минимум** в неотсортированной части и ставим его в конец отсортированной.

### Визуализация

```
Исходный: [64, 25, 12, 22, 11]
           ↑ отсортировано | неотсортировано

Шаг 1: минимум = 11 → меняем с 64
        [11, 25, 12, 22, 64]
         ✓

Шаг 2: минимум = 12 → меняем с 25  
        [11, 12, 25, 22, 64]
         ✓   ✓

Шаг 3: минимум = 22 → меняем с 25
        [11, 12, 22, 25, 64]
         ✓   ✓   ✓

Шаг 4: уже на месте
        [11, 12, 22, 25, 64]
         ✓   ✓   ✓   ✓   ✓
```

### Характеристики

| Свойство | Значение |
|----------|----------|
| Лучший случай | O(n²) — всегда проходит все элементы |
| Средний случай | O(n²) |
| Худший случай | O(n²) |
| Память | O(1) — in-place |
| Стабильность | ❌ Нет — swap может нарушить порядок равных |

### Плюсы и минусы

✅ **Плюсы:**
- Простой для понимания
- Минимум операций записи (ровно n-1 swap)
- Работает in-place

❌ **Минусы:**
- Всегда O(n²) — даже для отсортированных данных
- Нестабильный

### 📁 Пример кода

→ См. файл: [selection_sort.py](./selection_sort.py)

---

## 3. Insertion Sort (Сортировка вставками)

**Идея:** Считаем, что левая часть массива уже отсортирована. Берём следующий элемент и **вставляем** его в нужное место, сдвигая остальные.

### Метафора: Карты в руке 🃏

Представь, что ты сортируешь карты:
1. Первая карта — уже "отсортирована"
2. Берёшь следующую карту
3. Сравниваешь с картами в руке справа налево
4. Вставляешь в нужное место

### Визуализация

```
Исходный: [12, 11, 13, 5, 6]
           ✓  — считаем 12 отсортированным

Шаг 1: key=11, сравниваем с 12 → сдвигаем 12 → вставляем 11
        [11, 12, 13, 5, 6]
         ✓   ✓

Шаг 2: key=13, 13 > 12 → остаётся на месте
        [11, 12, 13, 5, 6]
         ✓   ✓   ✓

Шаг 3: key=5, сдвигаем 13, 12, 11 → вставляем 5
        [5, 11, 12, 13, 6]
         ✓  ✓   ✓   ✓

Шаг 4: key=6, сдвигаем 13, 12, 11 → вставляем 6
        [5, 6, 11, 12, 13]
         ✓  ✓  ✓   ✓   ✓
```

### Характеристики

| Свойство | Значение |
|----------|----------|
| Лучший случай | **O(n)** — уже отсортировано! |
| Средний случай | O(n²) |
| Худший случай | O(n²) — обратный порядок |
| Память | O(1) — in-place |
| Стабильность | ✅ Да |

### Плюсы и минусы

✅ **Плюсы:**
- **Лучший случай O(n)** — эффективен для почти отсортированных данных
- Стабильный
- Простая реализация
- Используется внутри Timsort для малых массивов!

❌ **Минусы:**
- O(n²) в среднем/худшем случае
- Много операций сдвига

### 📁 Пример кода

→ См. файл: [insertion_sort.py](./insertion_sort.py)

---

## Сравнение трёх алгоритмов

| Критерий | Bubble | Selection | Insertion |
|----------|--------|-----------|-----------|
| Лучший случай | O(n) | O(n²) | **O(n)** |
| Swaps в среднем | O(n²) | **O(n)** | O(n²) |
| Стабильность | ✅ | ❌ | ✅ |
| Адаптивность | Частичная | ❌ | ✅ |
| На практике | Худший | Средний | **Лучший** |

### Что значит "адаптивность"?

**Адаптивный** алгоритм работает быстрее на частично отсортированных данных.

- **Bubble sort** — частично адаптивен (можно остановиться раньше)
- **Selection sort** — не адаптивен (всегда O(n²))
- **Insertion sort** — полностью адаптивен (лучший случай O(n))

---

## Когда какой использовать?

| Ситуация | Рекомендация |
|----------|--------------|
| Учебные цели | Любой из трёх |
| Очень маленький массив (<10) | Insertion sort |
| Данные почти отсортированы | Insertion sort |
| Минимум операций записи | Selection sort |
| Продакшн | **sorted()** / **.sort()** |

---

## Почему в Python используется Timsort, а не эти?

**Timsort** — гибрид Merge sort и Insertion sort:
- Делит массив на "runs" (уже упорядоченные участки)
- Малые runs сортирует **Insertion sort** (как мы учили!)
- Большие runs сливает **Merge sort**

Так что Insertion sort — это не просто учебный алгоритм, он реально используется внутри Python!
